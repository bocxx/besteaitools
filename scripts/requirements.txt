# Python-deps voor de hero-image generator (scripts/generate-diorama.py).
# Wordt automatisch geïnstalleerd door scripts/diorama.sh in ./venv.
# Handmatig: python3 -m venv venv && venv/bin/pip install -r scripts/requirements.txt
requests>=2.31
Pillow>=10.0
